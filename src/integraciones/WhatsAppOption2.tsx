import WhatsAppOptionScreen from './WhatsAppOptionScreen'

// Option 2 (líder) — two checkboxes: antes / desde el 1 de octubre.
export default function WhatsAppOption2() {
  return <WhatsAppOptionScreen variant={2} />
}
